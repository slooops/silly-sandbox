# Optional: Add Supabase for persistent leaderboard
# Without these, scores are stored in localStorage with hardcoded friend scores
# Create a free project at https://supabase.com and add a "scores" table:
#   id (int8, auto), name (text), score (int4), emoji (text), created_at (timestamptz)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
